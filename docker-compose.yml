version: '3.8'

services:
  gennova-api:
    build: .
    container_name: gennova-api
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - .:/app
    restart: always
    networks:
      - gennova-network

networks:
  gennova-network:
    driver: bridge
